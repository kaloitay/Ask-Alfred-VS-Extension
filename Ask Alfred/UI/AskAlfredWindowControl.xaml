<UserControl x:Class="Ask_Alfred.UI.AskAlfredWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Name="MyToolWindow" Height="188.55" Width="626.232">
    <StackPanel Orientation="Vertical" Margin="0,0,-22.6,-0.2" HorizontalAlignment="Right" Width="649">
        <Button Content="Search!" Click="searchButton_Click" Width="120" Height="28" x:Name="searchButton" Margin="258,0,259,0" RenderTransformOrigin="0.526,-0.037"/>
        <DataGrid AutoGenerateColumns="False" x:Name="dataGridViewPages" Height="162" RenderTransformOrigin="0.714,0.537" SelectionChanged="DataGridViewPages_SelectionChanged" HorizontalAlignment="Right" Width="649" Margin="0,0,-0.2,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Subject" Binding="{Binding Subject}"/>
                <DataGridTextColumn Header="Created date" Binding="{Binding Date}"/>
                <DataGridTextColumn Header="View count" Binding="{Binding ViewCount}"/>
                <DataGridTextColumn Header="Score" Binding="{Binding Score}"/>
                <DataGridTextColumn Header="Is answered" Binding="{Binding IsAnswered}"/>
                <DataGridTextColumn Header="Favorite count" Binding="{Binding FavoriteCount}"/>
                <DataGridHyperlinkColumn Header="Url" Binding="{Binding Url}"/>
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</UserControl>
