<UserControl x:Class="Ask_Alfred.UI.AskAlfredWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Name="MyToolWindow"
             d:DesignWidth="500">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/AskAlfredResultStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <StackPanel VerticalAlignment="Stretch">
        <ComboBox x:Name="searchComboBox" IsEditable="True" KeyDown="SearchComboBox_KeyDown" Height="19" Margin="10,3,10.2,0" HorizontalAlignment="Stretch"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Image Height="7" Width="7" Margin="0,0,8,0"/>
            <TextBlock Text="Searching for itay kalo..."/>
        </StackPanel>
        <ScrollViewer HorizontalAlignment="Stretch" VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
            <Grid x:Name="mainResultsGrid">
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                    <RowDefinition Height="1*" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" HorizontalAlignment="Stretch">
                    <Border BorderThickness="1" Background="White">
                        <Image DockPanel.Dock="Left" Height="50" Width="40" />
                    </Border>
                    <TextBlock x:Name="DateTextBlock1" DockPanel.Dock="Top" HorizontalAlignment="Right" Text="23/09/2017" FontSize="5" Foreground="White" Margin="0,3,3,0"/>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Image Height="7" Width="7" Margin="0,0,8,0"/>
                        <TextBlock x:Name="WebsiteNameTextBlock1" Text="StackOverFlow" FontSize="7" Foreground="White" Margin="0,0,8,0"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Text="Keep mouse's scrollwheel to work on Scrollview when placed on top of Mapview/MapMarkerPopup and auto_dismiss" Margin="1,5,30,5" FontSize="9" Foreground="White" TextAlignment="Justify" VerticalAlignment="Center" FontFamily="Calibri Light"/>
                </DockPanel>
                <GridSplitter Grid.Row="0" Height="0.2" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" IsEnabled="False" Background="Transparent"/>

                <DockPanel Grid.Row="{Binding Path=NumOfResults}">
                    <Border BorderThickness="1" Background="White">
                        <Image DockPanel.Dock="Left" Height="50" Width="40" />
                    </Border>
                    <TextBlock x:Name="DateTextBlock2" DockPanel.Dock="Top" HorizontalAlignment="Right" Text="23/09/2017" FontSize="5" Foreground="White" Margin="0,3,3,0"/>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Image Height="7" Width="7" Margin="0,0,8,0"/>
                        <TextBlock x:Name="WebsiteNameTextBlock2" Text="StackOverFlow" FontSize="7" Foreground="White" Margin="0,0,8,0"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Text="123445546566534656" Margin="1,5,30,5" FontSize="9" Foreground="White" TextAlignment="Justify" VerticalAlignment="Center"/>
                </DockPanel>
                <GridSplitter Grid.Row="{Binding Path=NumOfResults}" Height="0.2" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" IsEnabled="False" Background="Transparent"/>

                <DockPanel Grid.Row="2">
                    <Border BorderThickness="1" Background="White">
                        <Image DockPanel.Dock="Left" Height="50" Width="40" />
                    </Border>
                    <TextBlock x:Name="DateTextBlock3" DockPanel.Dock="Top" HorizontalAlignment="Right" Text="23/09/2017" FontSize="5" Foreground="White" Margin="0,3,3,0"/>
                    <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Image Height="7" Width="7" Margin="0,0,8,0"/>
                        <TextBlock x:Name="WebsiteNameTextBlock3" Text="StackOverFlow" FontSize="7" Foreground="White" Margin="0,0,8,0"/>
                    </StackPanel>
                    <TextBlock TextWrapping="Wrap" Text="Keep mouse's scrollwheel to work on Scrollview when placed on top of Mapview/MapMarkerPopup and auto_dismiss" Margin="1,5,30,5" FontSize="9" Foreground="White" TextAlignment="Justify" VerticalAlignment="Center"/>
                </DockPanel>
                <GridSplitter Grid.Row="2" Height="0.2" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" IsEnabled="False" Background="Transparent"/>

            </Grid>
        </ScrollViewer>
    </StackPanel>
</UserControl>
